import { SearchLastUpdatedPipe } from './search-last-updated.pipe';

describe('SearchLastUpdatedPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLastUpdatedPipe();
    expect(pipe).toBeTruthy();
  });
});
