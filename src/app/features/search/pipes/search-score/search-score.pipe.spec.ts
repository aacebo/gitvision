import { SearchScorePipe } from './search-score.pipe';

describe('SearchScorePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchScorePipe();
    expect(pipe).toBeTruthy();
  });
});
