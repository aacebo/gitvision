import { IScrollDirective } from './i-scroll.directive';

describe('IScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new IScrollDirective();
    expect(directive).toBeTruthy();
  });
});
