import { LastPublishedPipe } from './last-published.pipe';

describe('LastPublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new LastPublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
